const router=require('express').Router(); const auth=require('../middlewares/auth.middleware'); const role=require('../middlewares/role.middleware'); const c=require('../controllers/espaco.controller'); router.use(auth); router.get('/',c.listar); router.get('/reservas',c.reservas); router.post('/',role('ADMIN'),c.criar); router.put('/:id',role('ADMIN'),c.atualizar); router.delete('/:id',role('ADMIN'),c.remover); router.post('/reservas',role('ADMIN','PSICOLOGO'),c.reservar); module.exports=router;
